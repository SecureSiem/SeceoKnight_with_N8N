1. As this Workflow fetch unread email from mailbox which we configure to recive Phishy emails from office employees. 
2. This workflow is design for gmail plateform for fetching latest unread email and then check IP agains abuseDB score, links check woth virustotal module and then
  the domain is verify with DMARK, DKIM and SFP records with dedicated node.
3. After analysing everything this AI workflow send us properly formated output in our slack channel which we configure at last node. 
